@model SimAuctionMVC.Controllers.LoginViewModel
@{
    ViewData["Title"] = "Đăng nhập";
}

<div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
        <div class="flex justify-center">
            <i class="fas fa-mobile-alt text-red-600 text-5xl"></i>
        </div>
        <h2 class="mt-6 text-center text-3xl font-bold text-gray-900">
            Đăng Nhập Tài Khoản
        </h2>
        <p class="mt-2 text-center text-sm text-gray-600">
            Hoặc 
            <a asp-action="Register" asp-route-returnUrl="@ViewData["ReturnUrl"]" class="font-medium text-red-600 hover:text-red-500">
                đăng ký tài khoản mới
            </a>
        </p>
    </div>

    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
        <div class="bg-white py-8 px-4 shadow-lg sm:rounded-lg sm:px-10">
            <!-- Demo Accounts -->
            <div class="mb-6 p-4 bg-blue-50 rounded-lg">
                <h3 class="text-sm font-medium text-blue-900 mb-2">Tài khoản demo:</h3>
                <div class="text-xs text-blue-700 space-y-1">
                    <div><strong>Quản trị viên:</strong> admin@sim.vn / Admin123!</div>
                    <div><strong>Người dùng:</strong> user@sim.vn / User123!</div>
                </div>
                <div class="mt-2 text-xs text-blue-600">
                    <i class="fas fa-info-circle mr-1"></i>
                    Tài khoản quản trị viên có quyền quản lý toàn bộ hệ thống
                </div>
            </div>

            <form asp-action="Login" method="post">
                <div asp-validation-summary="ModelOnly" class="text-red-600 mb-4"></div>
                
                <div class="mb-4">
                    <label asp-for="Email" class="block text-sm font-medium text-gray-700">Email</label>
                    <div class="mt-1">
                        <input asp-for="Email" class="appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-red-500 focus:border-red-500 sm:text-sm" placeholder="Nhập email của bạn" />
                        <span asp-validation-for="Email" class="text-red-600 text-sm"></span>
                    </div>
                </div>

                <div class="mb-4">
                    <label asp-for="Password" class="block text-sm font-medium text-gray-700">Mật khẩu</label>
                    <div class="mt-1">
                        <input asp-for="Password" class="appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-red-500 focus:border-red-500 sm:text-sm" placeholder="Nhập mật khẩu" />
                        <span asp-validation-for="Password" class="text-red-600 text-sm"></span>
                    </div>
                </div>

                <div class="flex items-center justify-between mb-6">
                    <div class="flex items-center">
                        <input asp-for="RememberMe" class="h-4 w-4 text-red-600 focus:ring-red-500 border-gray-300 rounded" />
                        <label asp-for="RememberMe" class="ml-2 block text-sm text-gray-900">
                            Ghi nhớ đăng nhập
                        </label>
                    </div>
                </div>

                <div>
                    <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                        Đăng Nhập
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>